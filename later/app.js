//import rx = require('rxjs/Rx');
//observable is stream, here we are making stream of a string we can create of anything
//like in second phase we will make stream of a request from server and click events
// rx.Observable.of('hello world').subscribe(function(data){
//     console.log("." + data)
// })
//subscribe is listener and we are passing a observer in it
//the observer would have 3 prameter data, error and complete for simplicity we are 
//just taking a data
//what if we want to have counter that how many times observer called 
//# sourceMappingURL=app.js.map